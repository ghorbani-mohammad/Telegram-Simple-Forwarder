image: docker:git

stages:
  - build
  - deploy

variables:
  DOCKER_TLS_CERTDIR: ""
  CONTAINER_RELEASE_IMAGE: registry.gitlab.com/ghorbani/bot:latest

build-image:
  stage: build
  only:
    - master@ghorbani/bot
  services:
    - docker:dind
  script:
    - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN registry.gitlab.com
    - docker pull $CONTAINER_RELEASE_IMAGE || true
    - docker build --cache-from $CONTAINER_RELEASE_IMAGE -t $CONTAINER_RELEASE_IMAGE .
    - docker push $CONTAINER_RELEASE_IMAGE

deploy-image:
  stage: deploy
  only:
    - master@ghorbani/bot
  script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - echo -e "Host target\n\tStrictHostKeyChecking no\n\tConnectTimeout 10\n\tConnectionAttempts 10\n\tHostName 20.20.20.20\n\tUser pourya\n\tPort 22\n" > ~/.ssh/config
    - chmod 600 ~/.ssh/config
    - scp docker-compose-pro.yml bot_api_nginx.conf mng-api.sh .docpasswd doc.json target:/
    - ssh target "cd / && ./mng-api.sh up"
